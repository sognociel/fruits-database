import React from "react";

const Home = () => {
  const homeStyle = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    width: "100vw",
    marginTop: "3rem",
    fontSize: "5rem",
  };
  return <div style={homeStyle}>🥝올해의 과일🫐</div>;
};

export default Home;
